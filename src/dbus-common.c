/*
 * Generated by gdbus-codegen 2.40.0. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "dbus-common.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface io.github.ghedo.grooved.Player
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:GroovedPlayer
 * @title: GroovedPlayer
 * @short_description: Generated C code for the io.github.ghedo.grooved.Player D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-io-github-ghedo-grooved-Player.top_of_page">io.github.ghedo.grooved.Player</link> D-Bus interface in C.
 */

/* ---- Introspection data for io.github.ghedo.grooved.Player ---- */

static const _ExtendedGDBusArgInfo _grooved_player_method_info_track_position_OUT_ARG_position =
{
  {
    -1,
    (gchar *) "position",
    (gchar *) "d",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _grooved_player_method_info_track_position_OUT_ARG_percent =
{
  {
    -1,
    (gchar *) "percent",
    (gchar *) "d",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _grooved_player_method_info_track_position_OUT_ARG_pointers[] =
{
  &_grooved_player_method_info_track_position_OUT_ARG_position,
  &_grooved_player_method_info_track_position_OUT_ARG_percent,
  NULL
};

static const _ExtendedGDBusMethodInfo _grooved_player_method_info_track_position =
{
  {
    -1,
    (gchar *) "TrackPosition",
    NULL,
    (GDBusArgInfo **) &_grooved_player_method_info_track_position_OUT_ARG_pointers,
    NULL
  },
  "handle-track-position",
  FALSE
};

static const _ExtendedGDBusMethodInfo _grooved_player_method_info_play =
{
  {
    -1,
    (gchar *) "Play",
    NULL,
    NULL,
    NULL
  },
  "handle-play",
  FALSE
};

static const _ExtendedGDBusMethodInfo _grooved_player_method_info_pause =
{
  {
    -1,
    (gchar *) "Pause",
    NULL,
    NULL,
    NULL
  },
  "handle-pause",
  FALSE
};

static const _ExtendedGDBusMethodInfo _grooved_player_method_info_toggle =
{
  {
    -1,
    (gchar *) "Toggle",
    NULL,
    NULL,
    NULL
  },
  "handle-toggle",
  FALSE
};

static const _ExtendedGDBusMethodInfo _grooved_player_method_info_next =
{
  {
    -1,
    (gchar *) "Next",
    NULL,
    NULL,
    NULL
  },
  "handle-next",
  FALSE
};

static const _ExtendedGDBusMethodInfo _grooved_player_method_info_prev =
{
  {
    -1,
    (gchar *) "Prev",
    NULL,
    NULL,
    NULL
  },
  "handle-prev",
  FALSE
};

static const _ExtendedGDBusMethodInfo _grooved_player_method_info_stop =
{
  {
    -1,
    (gchar *) "Stop",
    NULL,
    NULL,
    NULL
  },
  "handle-stop",
  FALSE
};

static const _ExtendedGDBusArgInfo _grooved_player_method_info_seek_IN_ARG_seconds =
{
  {
    -1,
    (gchar *) "seconds",
    (gchar *) "x",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _grooved_player_method_info_seek_IN_ARG_pointers[] =
{
  &_grooved_player_method_info_seek_IN_ARG_seconds,
  NULL
};

static const _ExtendedGDBusMethodInfo _grooved_player_method_info_seek =
{
  {
    -1,
    (gchar *) "Seek",
    (GDBusArgInfo **) &_grooved_player_method_info_seek_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-seek",
  FALSE
};

static const _ExtendedGDBusArgInfo _grooved_player_method_info_list_OUT_ARG_files =
{
  {
    -1,
    (gchar *) "files",
    (gchar *) "as",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _grooved_player_method_info_list_OUT_ARG_count =
{
  {
    -1,
    (gchar *) "count",
    (gchar *) "x",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _grooved_player_method_info_list_OUT_ARG_position =
{
  {
    -1,
    (gchar *) "position",
    (gchar *) "x",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _grooved_player_method_info_list_OUT_ARG_pointers[] =
{
  &_grooved_player_method_info_list_OUT_ARG_files,
  &_grooved_player_method_info_list_OUT_ARG_count,
  &_grooved_player_method_info_list_OUT_ARG_position,
  NULL
};

static const _ExtendedGDBusMethodInfo _grooved_player_method_info_list =
{
  {
    -1,
    (gchar *) "List",
    NULL,
    (GDBusArgInfo **) &_grooved_player_method_info_list_OUT_ARG_pointers,
    NULL
  },
  "handle-list",
  FALSE
};

static const _ExtendedGDBusArgInfo _grooved_player_method_info_add_track_IN_ARG_path =
{
  {
    -1,
    (gchar *) "path",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _grooved_player_method_info_add_track_IN_ARG_pointers[] =
{
  &_grooved_player_method_info_add_track_IN_ARG_path,
  NULL
};

static const _ExtendedGDBusMethodInfo _grooved_player_method_info_add_track =
{
  {
    -1,
    (gchar *) "AddTrack",
    (GDBusArgInfo **) &_grooved_player_method_info_add_track_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-add-track",
  FALSE
};

static const _ExtendedGDBusArgInfo _grooved_player_method_info_add_list_IN_ARG_path =
{
  {
    -1,
    (gchar *) "path",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _grooved_player_method_info_add_list_IN_ARG_pointers[] =
{
  &_grooved_player_method_info_add_list_IN_ARG_path,
  NULL
};

static const _ExtendedGDBusMethodInfo _grooved_player_method_info_add_list =
{
  {
    -1,
    (gchar *) "AddList",
    (GDBusArgInfo **) &_grooved_player_method_info_add_list_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-add-list",
  FALSE
};

static const _ExtendedGDBusArgInfo _grooved_player_method_info_goto_track_IN_ARG_index =
{
  {
    -1,
    (gchar *) "index",
    (gchar *) "t",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _grooved_player_method_info_goto_track_IN_ARG_pointers[] =
{
  &_grooved_player_method_info_goto_track_IN_ARG_index,
  NULL
};

static const _ExtendedGDBusMethodInfo _grooved_player_method_info_goto_track =
{
  {
    -1,
    (gchar *) "GotoTrack",
    (GDBusArgInfo **) &_grooved_player_method_info_goto_track_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-goto-track",
  FALSE
};

static const _ExtendedGDBusArgInfo _grooved_player_method_info_remove_track_IN_ARG_index =
{
  {
    -1,
    (gchar *) "index",
    (gchar *) "x",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _grooved_player_method_info_remove_track_IN_ARG_pointers[] =
{
  &_grooved_player_method_info_remove_track_IN_ARG_index,
  NULL
};

static const _ExtendedGDBusMethodInfo _grooved_player_method_info_remove_track =
{
  {
    -1,
    (gchar *) "RemoveTrack",
    (GDBusArgInfo **) &_grooved_player_method_info_remove_track_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-remove-track",
  FALSE
};

static const _ExtendedGDBusArgInfo _grooved_player_method_info_loop_IN_ARG_mode =
{
  {
    -1,
    (gchar *) "mode",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _grooved_player_method_info_loop_IN_ARG_pointers[] =
{
  &_grooved_player_method_info_loop_IN_ARG_mode,
  NULL
};

static const _ExtendedGDBusMethodInfo _grooved_player_method_info_loop =
{
  {
    -1,
    (gchar *) "Loop",
    (GDBusArgInfo **) &_grooved_player_method_info_loop_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-loop",
  FALSE
};

static const _ExtendedGDBusMethodInfo _grooved_player_method_info_quit =
{
  {
    -1,
    (gchar *) "Quit",
    NULL,
    NULL,
    NULL
  },
  "handle-quit",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _grooved_player_method_info_pointers[] =
{
  &_grooved_player_method_info_track_position,
  &_grooved_player_method_info_play,
  &_grooved_player_method_info_pause,
  &_grooved_player_method_info_toggle,
  &_grooved_player_method_info_next,
  &_grooved_player_method_info_prev,
  &_grooved_player_method_info_stop,
  &_grooved_player_method_info_seek,
  &_grooved_player_method_info_list,
  &_grooved_player_method_info_add_track,
  &_grooved_player_method_info_add_list,
  &_grooved_player_method_info_goto_track,
  &_grooved_player_method_info_remove_track,
  &_grooved_player_method_info_loop,
  &_grooved_player_method_info_quit,
  NULL
};

static const _ExtendedGDBusSignalInfo _grooved_player_signal_info_status_changed =
{
  {
    -1,
    (gchar *) "StatusChanged",
    NULL,
    NULL
  },
  "status-changed"
};

static const _ExtendedGDBusSignalInfo _grooved_player_signal_info_track_changed =
{
  {
    -1,
    (gchar *) "TrackChanged",
    NULL,
    NULL
  },
  "track-changed"
};

static const _ExtendedGDBusSignalInfo _grooved_player_signal_info_option_changed =
{
  {
    -1,
    (gchar *) "OptionChanged",
    NULL,
    NULL
  },
  "option-changed"
};

static const _ExtendedGDBusSignalInfo * const _grooved_player_signal_info_pointers[] =
{
  &_grooved_player_signal_info_status_changed,
  &_grooved_player_signal_info_track_changed,
  &_grooved_player_signal_info_option_changed,
  NULL
};

static const _ExtendedGDBusPropertyInfo _grooved_player_property_info_playback_status =
{
  {
    -1,
    (gchar *) "PlaybackStatus",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "playback-status",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _grooved_player_property_info_loop_status =
{
  {
    -1,
    (gchar *) "LoopStatus",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "loop-status",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _grooved_player_property_info_track_metadata =
{
  {
    -1,
    (gchar *) "TrackMetadata",
    (gchar *) "a{ss}",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "track-metadata",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _grooved_player_property_info_track_path =
{
  {
    -1,
    (gchar *) "TrackPath",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "track-path",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _grooved_player_property_info_track_length =
{
  {
    -1,
    (gchar *) "TrackLength",
    (gchar *) "d",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "track-length",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _grooved_player_property_info_pointers[] =
{
  &_grooved_player_property_info_playback_status,
  &_grooved_player_property_info_loop_status,
  &_grooved_player_property_info_track_metadata,
  &_grooved_player_property_info_track_path,
  &_grooved_player_property_info_track_length,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _grooved_player_interface_info =
{
  {
    -1,
    (gchar *) "io.github.ghedo.grooved.Player",
    (GDBusMethodInfo **) &_grooved_player_method_info_pointers,
    (GDBusSignalInfo **) &_grooved_player_signal_info_pointers,
    (GDBusPropertyInfo **) &_grooved_player_property_info_pointers,
    NULL
  },
  "player",
};


/**
 * grooved_player_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-io-github-ghedo-grooved-Player.top_of_page">io.github.ghedo.grooved.Player</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
grooved_player_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_grooved_player_interface_info.parent_struct;
}

/**
 * grooved_player_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #GroovedPlayer interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
grooved_player_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "playback-status");
  g_object_class_override_property (klass, property_id_begin++, "loop-status");
  g_object_class_override_property (klass, property_id_begin++, "track-metadata");
  g_object_class_override_property (klass, property_id_begin++, "track-path");
  g_object_class_override_property (klass, property_id_begin++, "track-length");
  return property_id_begin - 1;
}



/**
 * GroovedPlayer:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-io-github-ghedo-grooved-Player.top_of_page">io.github.ghedo.grooved.Player</link>.
 */

/**
 * GroovedPlayerIface:
 * @parent_iface: The parent interface.
 * @handle_add_list: Handler for the #GroovedPlayer::handle-add-list signal.
 * @handle_add_track: Handler for the #GroovedPlayer::handle-add-track signal.
 * @handle_goto_track: Handler for the #GroovedPlayer::handle-goto-track signal.
 * @handle_list: Handler for the #GroovedPlayer::handle-list signal.
 * @handle_loop: Handler for the #GroovedPlayer::handle-loop signal.
 * @handle_next: Handler for the #GroovedPlayer::handle-next signal.
 * @handle_pause: Handler for the #GroovedPlayer::handle-pause signal.
 * @handle_play: Handler for the #GroovedPlayer::handle-play signal.
 * @handle_prev: Handler for the #GroovedPlayer::handle-prev signal.
 * @handle_quit: Handler for the #GroovedPlayer::handle-quit signal.
 * @handle_remove_track: Handler for the #GroovedPlayer::handle-remove-track signal.
 * @handle_seek: Handler for the #GroovedPlayer::handle-seek signal.
 * @handle_stop: Handler for the #GroovedPlayer::handle-stop signal.
 * @handle_toggle: Handler for the #GroovedPlayer::handle-toggle signal.
 * @handle_track_position: Handler for the #GroovedPlayer::handle-track-position signal.
 * @get_loop_status: Getter for the #GroovedPlayer:loop-status property.
 * @get_playback_status: Getter for the #GroovedPlayer:playback-status property.
 * @get_track_length: Getter for the #GroovedPlayer:track-length property.
 * @get_track_metadata: Getter for the #GroovedPlayer:track-metadata property.
 * @get_track_path: Getter for the #GroovedPlayer:track-path property.
 * @option_changed: Handler for the #GroovedPlayer::option-changed signal.
 * @status_changed: Handler for the #GroovedPlayer::status-changed signal.
 * @track_changed: Handler for the #GroovedPlayer::track-changed signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-io-github-ghedo-grooved-Player.top_of_page">io.github.ghedo.grooved.Player</link>.
 */

typedef GroovedPlayerIface GroovedPlayerInterface;
G_DEFINE_INTERFACE (GroovedPlayer, grooved_player, G_TYPE_OBJECT);

static void
grooved_player_default_init (GroovedPlayerIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * GroovedPlayer::handle-track-position:
   * @object: A #GroovedPlayer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.TrackPosition">TrackPosition()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call grooved_player_complete_track_position() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-track-position",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GroovedPlayerIface, handle_track_position),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * GroovedPlayer::handle-play:
   * @object: A #GroovedPlayer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Play">Play()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call grooved_player_complete_play() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-play",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GroovedPlayerIface, handle_play),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * GroovedPlayer::handle-pause:
   * @object: A #GroovedPlayer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Pause">Pause()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call grooved_player_complete_pause() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-pause",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GroovedPlayerIface, handle_pause),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * GroovedPlayer::handle-toggle:
   * @object: A #GroovedPlayer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Toggle">Toggle()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call grooved_player_complete_toggle() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-toggle",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GroovedPlayerIface, handle_toggle),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * GroovedPlayer::handle-next:
   * @object: A #GroovedPlayer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Next">Next()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call grooved_player_complete_next() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-next",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GroovedPlayerIface, handle_next),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * GroovedPlayer::handle-prev:
   * @object: A #GroovedPlayer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Prev">Prev()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call grooved_player_complete_prev() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-prev",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GroovedPlayerIface, handle_prev),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * GroovedPlayer::handle-stop:
   * @object: A #GroovedPlayer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Stop">Stop()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call grooved_player_complete_stop() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-stop",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GroovedPlayerIface, handle_stop),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * GroovedPlayer::handle-seek:
   * @object: A #GroovedPlayer.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_seconds: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Seek">Seek()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call grooved_player_complete_seek() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-seek",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GroovedPlayerIface, handle_seek),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_INT64);

  /**
   * GroovedPlayer::handle-list:
   * @object: A #GroovedPlayer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.List">List()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call grooved_player_complete_list() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-list",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GroovedPlayerIface, handle_list),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * GroovedPlayer::handle-add-track:
   * @object: A #GroovedPlayer.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_path: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.AddTrack">AddTrack()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call grooved_player_complete_add_track() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-add-track",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GroovedPlayerIface, handle_add_track),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * GroovedPlayer::handle-add-list:
   * @object: A #GroovedPlayer.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_path: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.AddList">AddList()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call grooved_player_complete_add_list() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-add-list",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GroovedPlayerIface, handle_add_list),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * GroovedPlayer::handle-goto-track:
   * @object: A #GroovedPlayer.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_index: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.GotoTrack">GotoTrack()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call grooved_player_complete_goto_track() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-goto-track",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GroovedPlayerIface, handle_goto_track),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT64);

  /**
   * GroovedPlayer::handle-remove-track:
   * @object: A #GroovedPlayer.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_index: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.RemoveTrack">RemoveTrack()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call grooved_player_complete_remove_track() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-remove-track",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GroovedPlayerIface, handle_remove_track),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_INT64);

  /**
   * GroovedPlayer::handle-loop:
   * @object: A #GroovedPlayer.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_mode: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Loop">Loop()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call grooved_player_complete_loop() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-loop",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GroovedPlayerIface, handle_loop),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * GroovedPlayer::handle-quit:
   * @object: A #GroovedPlayer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Quit">Quit()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call grooved_player_complete_quit() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-quit",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GroovedPlayerIface, handle_quit),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject signals for received D-Bus signals: */
  /**
   * GroovedPlayer::status-changed:
   * @object: A #GroovedPlayer.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-io-github-ghedo-grooved-Player.StatusChanged">"StatusChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("status-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GroovedPlayerIface, status_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    0);

  /**
   * GroovedPlayer::track-changed:
   * @object: A #GroovedPlayer.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-io-github-ghedo-grooved-Player.TrackChanged">"TrackChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("track-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GroovedPlayerIface, track_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    0);

  /**
   * GroovedPlayer::option-changed:
   * @object: A #GroovedPlayer.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-io-github-ghedo-grooved-Player.OptionChanged">"OptionChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("option-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GroovedPlayerIface, option_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    0);

  /* GObject properties for D-Bus properties: */
  /**
   * GroovedPlayer:playback-status:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-io-github-ghedo-grooved-Player.PlaybackStatus">"PlaybackStatus"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("playback-status", "PlaybackStatus", "PlaybackStatus", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * GroovedPlayer:loop-status:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-io-github-ghedo-grooved-Player.LoopStatus">"LoopStatus"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("loop-status", "LoopStatus", "LoopStatus", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * GroovedPlayer:track-metadata:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-io-github-ghedo-grooved-Player.TrackMetadata">"TrackMetadata"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_variant ("track-metadata", "TrackMetadata", "TrackMetadata", G_VARIANT_TYPE ("a{ss}"), NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * GroovedPlayer:track-path:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-io-github-ghedo-grooved-Player.TrackPath">"TrackPath"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("track-path", "TrackPath", "TrackPath", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * GroovedPlayer:track-length:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-io-github-ghedo-grooved-Player.TrackLength">"TrackLength"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_double ("track-length", "TrackLength", "TrackLength", -G_MAXDOUBLE, G_MAXDOUBLE, 0.0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * grooved_player_get_playback_status: (skip)
 * @object: A #GroovedPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-io-github-ghedo-grooved-Player.PlaybackStatus">"PlaybackStatus"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use grooved_player_dup_playback_status() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
grooved_player_get_playback_status (GroovedPlayer *object)
{
  return GROOVED_PLAYER_GET_IFACE (object)->get_playback_status (object);
}

/**
 * grooved_player_dup_playback_status: (skip)
 * @object: A #GroovedPlayer.
 *
 * Gets a copy of the <link linkend="gdbus-property-io-github-ghedo-grooved-Player.PlaybackStatus">"PlaybackStatus"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
grooved_player_dup_playback_status (GroovedPlayer *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "playback-status", &value, NULL);
  return value;
}

/**
 * grooved_player_set_playback_status: (skip)
 * @object: A #GroovedPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-io-github-ghedo-grooved-Player.PlaybackStatus">"PlaybackStatus"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
grooved_player_set_playback_status (GroovedPlayer *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "playback-status", value, NULL);
}

/**
 * grooved_player_get_loop_status: (skip)
 * @object: A #GroovedPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-io-github-ghedo-grooved-Player.LoopStatus">"LoopStatus"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use grooved_player_dup_loop_status() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
grooved_player_get_loop_status (GroovedPlayer *object)
{
  return GROOVED_PLAYER_GET_IFACE (object)->get_loop_status (object);
}

/**
 * grooved_player_dup_loop_status: (skip)
 * @object: A #GroovedPlayer.
 *
 * Gets a copy of the <link linkend="gdbus-property-io-github-ghedo-grooved-Player.LoopStatus">"LoopStatus"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
grooved_player_dup_loop_status (GroovedPlayer *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "loop-status", &value, NULL);
  return value;
}

/**
 * grooved_player_set_loop_status: (skip)
 * @object: A #GroovedPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-io-github-ghedo-grooved-Player.LoopStatus">"LoopStatus"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
grooved_player_set_loop_status (GroovedPlayer *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "loop-status", value, NULL);
}

/**
 * grooved_player_get_track_metadata: (skip)
 * @object: A #GroovedPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-io-github-ghedo-grooved-Player.TrackMetadata">"TrackMetadata"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use grooved_player_dup_track_metadata() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
GVariant *
grooved_player_get_track_metadata (GroovedPlayer *object)
{
  return GROOVED_PLAYER_GET_IFACE (object)->get_track_metadata (object);
}

/**
 * grooved_player_dup_track_metadata: (skip)
 * @object: A #GroovedPlayer.
 *
 * Gets a copy of the <link linkend="gdbus-property-io-github-ghedo-grooved-Player.TrackMetadata">"TrackMetadata"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_variant_unref().
 */
GVariant *
grooved_player_dup_track_metadata (GroovedPlayer *object)
{
  GVariant *value;
  g_object_get (G_OBJECT (object), "track-metadata", &value, NULL);
  return value;
}

/**
 * grooved_player_set_track_metadata: (skip)
 * @object: A #GroovedPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-io-github-ghedo-grooved-Player.TrackMetadata">"TrackMetadata"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
grooved_player_set_track_metadata (GroovedPlayer *object, GVariant *value)
{
  g_object_set (G_OBJECT (object), "track-metadata", value, NULL);
}

/**
 * grooved_player_get_track_path: (skip)
 * @object: A #GroovedPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-io-github-ghedo-grooved-Player.TrackPath">"TrackPath"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use grooved_player_dup_track_path() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
grooved_player_get_track_path (GroovedPlayer *object)
{
  return GROOVED_PLAYER_GET_IFACE (object)->get_track_path (object);
}

/**
 * grooved_player_dup_track_path: (skip)
 * @object: A #GroovedPlayer.
 *
 * Gets a copy of the <link linkend="gdbus-property-io-github-ghedo-grooved-Player.TrackPath">"TrackPath"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
grooved_player_dup_track_path (GroovedPlayer *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "track-path", &value, NULL);
  return value;
}

/**
 * grooved_player_set_track_path: (skip)
 * @object: A #GroovedPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-io-github-ghedo-grooved-Player.TrackPath">"TrackPath"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
grooved_player_set_track_path (GroovedPlayer *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "track-path", value, NULL);
}

/**
 * grooved_player_get_track_length: (skip)
 * @object: A #GroovedPlayer.
 *
 * Gets the value of the <link linkend="gdbus-property-io-github-ghedo-grooved-Player.TrackLength">"TrackLength"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gdouble
grooved_player_get_track_length (GroovedPlayer *object)
{
  return GROOVED_PLAYER_GET_IFACE (object)->get_track_length (object);
}

/**
 * grooved_player_set_track_length: (skip)
 * @object: A #GroovedPlayer.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-io-github-ghedo-grooved-Player.TrackLength">"TrackLength"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
grooved_player_set_track_length (GroovedPlayer *object, gdouble value)
{
  g_object_set (G_OBJECT (object), "track-length", value, NULL);
}

/**
 * grooved_player_emit_status_changed:
 * @object: A #GroovedPlayer.
 *
 * Emits the <link linkend="gdbus-signal-io-github-ghedo-grooved-Player.StatusChanged">"StatusChanged"</link> D-Bus signal.
 */
void
grooved_player_emit_status_changed (
    GroovedPlayer *object)
{
  g_signal_emit_by_name (object, "status-changed");
}

/**
 * grooved_player_emit_track_changed:
 * @object: A #GroovedPlayer.
 *
 * Emits the <link linkend="gdbus-signal-io-github-ghedo-grooved-Player.TrackChanged">"TrackChanged"</link> D-Bus signal.
 */
void
grooved_player_emit_track_changed (
    GroovedPlayer *object)
{
  g_signal_emit_by_name (object, "track-changed");
}

/**
 * grooved_player_emit_option_changed:
 * @object: A #GroovedPlayer.
 *
 * Emits the <link linkend="gdbus-signal-io-github-ghedo-grooved-Player.OptionChanged">"OptionChanged"</link> D-Bus signal.
 */
void
grooved_player_emit_option_changed (
    GroovedPlayer *object)
{
  g_signal_emit_by_name (object, "option-changed");
}

/**
 * grooved_player_call_track_position:
 * @proxy: A #GroovedPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.TrackPosition">TrackPosition()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call grooved_player_call_track_position_finish() to get the result of the operation.
 *
 * See grooved_player_call_track_position_sync() for the synchronous, blocking version of this method.
 */
void
grooved_player_call_track_position (
    GroovedPlayer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "TrackPosition",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * grooved_player_call_track_position_finish:
 * @proxy: A #GroovedPlayerProxy.
 * @out_position: (out): Return location for return parameter or %NULL to ignore.
 * @out_percent: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to grooved_player_call_track_position().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with grooved_player_call_track_position().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_track_position_finish (
    GroovedPlayer *proxy,
    gdouble *out_position,
    gdouble *out_percent,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(dd)",
                 out_position,
                 out_percent);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_track_position_sync:
 * @proxy: A #GroovedPlayerProxy.
 * @out_position: (out): Return location for return parameter or %NULL to ignore.
 * @out_percent: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.TrackPosition">TrackPosition()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See grooved_player_call_track_position() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_track_position_sync (
    GroovedPlayer *proxy,
    gdouble *out_position,
    gdouble *out_percent,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "TrackPosition",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(dd)",
                 out_position,
                 out_percent);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_play:
 * @proxy: A #GroovedPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Play">Play()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call grooved_player_call_play_finish() to get the result of the operation.
 *
 * See grooved_player_call_play_sync() for the synchronous, blocking version of this method.
 */
void
grooved_player_call_play (
    GroovedPlayer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Play",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * grooved_player_call_play_finish:
 * @proxy: A #GroovedPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to grooved_player_call_play().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with grooved_player_call_play().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_play_finish (
    GroovedPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_play_sync:
 * @proxy: A #GroovedPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Play">Play()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See grooved_player_call_play() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_play_sync (
    GroovedPlayer *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Play",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_pause:
 * @proxy: A #GroovedPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Pause">Pause()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call grooved_player_call_pause_finish() to get the result of the operation.
 *
 * See grooved_player_call_pause_sync() for the synchronous, blocking version of this method.
 */
void
grooved_player_call_pause (
    GroovedPlayer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Pause",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * grooved_player_call_pause_finish:
 * @proxy: A #GroovedPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to grooved_player_call_pause().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with grooved_player_call_pause().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_pause_finish (
    GroovedPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_pause_sync:
 * @proxy: A #GroovedPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Pause">Pause()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See grooved_player_call_pause() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_pause_sync (
    GroovedPlayer *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Pause",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_toggle:
 * @proxy: A #GroovedPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Toggle">Toggle()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call grooved_player_call_toggle_finish() to get the result of the operation.
 *
 * See grooved_player_call_toggle_sync() for the synchronous, blocking version of this method.
 */
void
grooved_player_call_toggle (
    GroovedPlayer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Toggle",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * grooved_player_call_toggle_finish:
 * @proxy: A #GroovedPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to grooved_player_call_toggle().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with grooved_player_call_toggle().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_toggle_finish (
    GroovedPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_toggle_sync:
 * @proxy: A #GroovedPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Toggle">Toggle()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See grooved_player_call_toggle() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_toggle_sync (
    GroovedPlayer *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Toggle",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_next:
 * @proxy: A #GroovedPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Next">Next()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call grooved_player_call_next_finish() to get the result of the operation.
 *
 * See grooved_player_call_next_sync() for the synchronous, blocking version of this method.
 */
void
grooved_player_call_next (
    GroovedPlayer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Next",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * grooved_player_call_next_finish:
 * @proxy: A #GroovedPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to grooved_player_call_next().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with grooved_player_call_next().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_next_finish (
    GroovedPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_next_sync:
 * @proxy: A #GroovedPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Next">Next()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See grooved_player_call_next() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_next_sync (
    GroovedPlayer *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Next",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_prev:
 * @proxy: A #GroovedPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Prev">Prev()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call grooved_player_call_prev_finish() to get the result of the operation.
 *
 * See grooved_player_call_prev_sync() for the synchronous, blocking version of this method.
 */
void
grooved_player_call_prev (
    GroovedPlayer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Prev",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * grooved_player_call_prev_finish:
 * @proxy: A #GroovedPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to grooved_player_call_prev().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with grooved_player_call_prev().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_prev_finish (
    GroovedPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_prev_sync:
 * @proxy: A #GroovedPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Prev">Prev()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See grooved_player_call_prev() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_prev_sync (
    GroovedPlayer *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Prev",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_stop:
 * @proxy: A #GroovedPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Stop">Stop()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call grooved_player_call_stop_finish() to get the result of the operation.
 *
 * See grooved_player_call_stop_sync() for the synchronous, blocking version of this method.
 */
void
grooved_player_call_stop (
    GroovedPlayer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Stop",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * grooved_player_call_stop_finish:
 * @proxy: A #GroovedPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to grooved_player_call_stop().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with grooved_player_call_stop().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_stop_finish (
    GroovedPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_stop_sync:
 * @proxy: A #GroovedPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Stop">Stop()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See grooved_player_call_stop() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_stop_sync (
    GroovedPlayer *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Stop",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_seek:
 * @proxy: A #GroovedPlayerProxy.
 * @arg_seconds: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Seek">Seek()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call grooved_player_call_seek_finish() to get the result of the operation.
 *
 * See grooved_player_call_seek_sync() for the synchronous, blocking version of this method.
 */
void
grooved_player_call_seek (
    GroovedPlayer *proxy,
    gint64 arg_seconds,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Seek",
    g_variant_new ("(x)",
                   arg_seconds),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * grooved_player_call_seek_finish:
 * @proxy: A #GroovedPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to grooved_player_call_seek().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with grooved_player_call_seek().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_seek_finish (
    GroovedPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_seek_sync:
 * @proxy: A #GroovedPlayerProxy.
 * @arg_seconds: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Seek">Seek()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See grooved_player_call_seek() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_seek_sync (
    GroovedPlayer *proxy,
    gint64 arg_seconds,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Seek",
    g_variant_new ("(x)",
                   arg_seconds),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_list:
 * @proxy: A #GroovedPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.List">List()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call grooved_player_call_list_finish() to get the result of the operation.
 *
 * See grooved_player_call_list_sync() for the synchronous, blocking version of this method.
 */
void
grooved_player_call_list (
    GroovedPlayer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "List",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * grooved_player_call_list_finish:
 * @proxy: A #GroovedPlayerProxy.
 * @out_files: (out): Return location for return parameter or %NULL to ignore.
 * @out_count: (out): Return location for return parameter or %NULL to ignore.
 * @out_position: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to grooved_player_call_list().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with grooved_player_call_list().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_list_finish (
    GroovedPlayer *proxy,
    gchar ***out_files,
    gint64 *out_count,
    gint64 *out_position,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(^asxx)",
                 out_files,
                 out_count,
                 out_position);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_list_sync:
 * @proxy: A #GroovedPlayerProxy.
 * @out_files: (out): Return location for return parameter or %NULL to ignore.
 * @out_count: (out): Return location for return parameter or %NULL to ignore.
 * @out_position: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.List">List()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See grooved_player_call_list() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_list_sync (
    GroovedPlayer *proxy,
    gchar ***out_files,
    gint64 *out_count,
    gint64 *out_position,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "List",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(^asxx)",
                 out_files,
                 out_count,
                 out_position);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_add_track:
 * @proxy: A #GroovedPlayerProxy.
 * @arg_path: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.AddTrack">AddTrack()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call grooved_player_call_add_track_finish() to get the result of the operation.
 *
 * See grooved_player_call_add_track_sync() for the synchronous, blocking version of this method.
 */
void
grooved_player_call_add_track (
    GroovedPlayer *proxy,
    const gchar *arg_path,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "AddTrack",
    g_variant_new ("(s)",
                   arg_path),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * grooved_player_call_add_track_finish:
 * @proxy: A #GroovedPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to grooved_player_call_add_track().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with grooved_player_call_add_track().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_add_track_finish (
    GroovedPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_add_track_sync:
 * @proxy: A #GroovedPlayerProxy.
 * @arg_path: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.AddTrack">AddTrack()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See grooved_player_call_add_track() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_add_track_sync (
    GroovedPlayer *proxy,
    const gchar *arg_path,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "AddTrack",
    g_variant_new ("(s)",
                   arg_path),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_add_list:
 * @proxy: A #GroovedPlayerProxy.
 * @arg_path: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.AddList">AddList()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call grooved_player_call_add_list_finish() to get the result of the operation.
 *
 * See grooved_player_call_add_list_sync() for the synchronous, blocking version of this method.
 */
void
grooved_player_call_add_list (
    GroovedPlayer *proxy,
    const gchar *arg_path,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "AddList",
    g_variant_new ("(s)",
                   arg_path),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * grooved_player_call_add_list_finish:
 * @proxy: A #GroovedPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to grooved_player_call_add_list().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with grooved_player_call_add_list().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_add_list_finish (
    GroovedPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_add_list_sync:
 * @proxy: A #GroovedPlayerProxy.
 * @arg_path: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.AddList">AddList()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See grooved_player_call_add_list() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_add_list_sync (
    GroovedPlayer *proxy,
    const gchar *arg_path,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "AddList",
    g_variant_new ("(s)",
                   arg_path),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_goto_track:
 * @proxy: A #GroovedPlayerProxy.
 * @arg_index: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.GotoTrack">GotoTrack()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call grooved_player_call_goto_track_finish() to get the result of the operation.
 *
 * See grooved_player_call_goto_track_sync() for the synchronous, blocking version of this method.
 */
void
grooved_player_call_goto_track (
    GroovedPlayer *proxy,
    guint64 arg_index,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GotoTrack",
    g_variant_new ("(t)",
                   arg_index),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * grooved_player_call_goto_track_finish:
 * @proxy: A #GroovedPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to grooved_player_call_goto_track().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with grooved_player_call_goto_track().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_goto_track_finish (
    GroovedPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_goto_track_sync:
 * @proxy: A #GroovedPlayerProxy.
 * @arg_index: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.GotoTrack">GotoTrack()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See grooved_player_call_goto_track() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_goto_track_sync (
    GroovedPlayer *proxy,
    guint64 arg_index,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GotoTrack",
    g_variant_new ("(t)",
                   arg_index),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_remove_track:
 * @proxy: A #GroovedPlayerProxy.
 * @arg_index: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.RemoveTrack">RemoveTrack()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call grooved_player_call_remove_track_finish() to get the result of the operation.
 *
 * See grooved_player_call_remove_track_sync() for the synchronous, blocking version of this method.
 */
void
grooved_player_call_remove_track (
    GroovedPlayer *proxy,
    gint64 arg_index,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "RemoveTrack",
    g_variant_new ("(x)",
                   arg_index),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * grooved_player_call_remove_track_finish:
 * @proxy: A #GroovedPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to grooved_player_call_remove_track().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with grooved_player_call_remove_track().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_remove_track_finish (
    GroovedPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_remove_track_sync:
 * @proxy: A #GroovedPlayerProxy.
 * @arg_index: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.RemoveTrack">RemoveTrack()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See grooved_player_call_remove_track() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_remove_track_sync (
    GroovedPlayer *proxy,
    gint64 arg_index,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "RemoveTrack",
    g_variant_new ("(x)",
                   arg_index),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_loop:
 * @proxy: A #GroovedPlayerProxy.
 * @arg_mode: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Loop">Loop()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call grooved_player_call_loop_finish() to get the result of the operation.
 *
 * See grooved_player_call_loop_sync() for the synchronous, blocking version of this method.
 */
void
grooved_player_call_loop (
    GroovedPlayer *proxy,
    const gchar *arg_mode,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Loop",
    g_variant_new ("(s)",
                   arg_mode),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * grooved_player_call_loop_finish:
 * @proxy: A #GroovedPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to grooved_player_call_loop().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with grooved_player_call_loop().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_loop_finish (
    GroovedPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_loop_sync:
 * @proxy: A #GroovedPlayerProxy.
 * @arg_mode: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Loop">Loop()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See grooved_player_call_loop() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_loop_sync (
    GroovedPlayer *proxy,
    const gchar *arg_mode,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Loop",
    g_variant_new ("(s)",
                   arg_mode),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_quit:
 * @proxy: A #GroovedPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Quit">Quit()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call grooved_player_call_quit_finish() to get the result of the operation.
 *
 * See grooved_player_call_quit_sync() for the synchronous, blocking version of this method.
 */
void
grooved_player_call_quit (
    GroovedPlayer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Quit",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * grooved_player_call_quit_finish:
 * @proxy: A #GroovedPlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to grooved_player_call_quit().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with grooved_player_call_quit().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_quit_finish (
    GroovedPlayer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_call_quit_sync:
 * @proxy: A #GroovedPlayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Quit">Quit()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See grooved_player_call_quit() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
grooved_player_call_quit_sync (
    GroovedPlayer *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Quit",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * grooved_player_complete_track_position:
 * @object: A #GroovedPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @position: Parameter to return.
 * @percent: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.TrackPosition">TrackPosition()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
grooved_player_complete_track_position (
    GroovedPlayer *object,
    GDBusMethodInvocation *invocation,
    gdouble position,
    gdouble percent)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(dd)",
                   position,
                   percent));
}

/**
 * grooved_player_complete_play:
 * @object: A #GroovedPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Play">Play()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
grooved_player_complete_play (
    GroovedPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * grooved_player_complete_pause:
 * @object: A #GroovedPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Pause">Pause()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
grooved_player_complete_pause (
    GroovedPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * grooved_player_complete_toggle:
 * @object: A #GroovedPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Toggle">Toggle()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
grooved_player_complete_toggle (
    GroovedPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * grooved_player_complete_next:
 * @object: A #GroovedPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Next">Next()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
grooved_player_complete_next (
    GroovedPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * grooved_player_complete_prev:
 * @object: A #GroovedPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Prev">Prev()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
grooved_player_complete_prev (
    GroovedPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * grooved_player_complete_stop:
 * @object: A #GroovedPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Stop">Stop()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
grooved_player_complete_stop (
    GroovedPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * grooved_player_complete_seek:
 * @object: A #GroovedPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Seek">Seek()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
grooved_player_complete_seek (
    GroovedPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * grooved_player_complete_list:
 * @object: A #GroovedPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @files: Parameter to return.
 * @count: Parameter to return.
 * @position: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.List">List()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
grooved_player_complete_list (
    GroovedPlayer *object,
    GDBusMethodInvocation *invocation,
    const gchar *const *files,
    gint64 count,
    gint64 position)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(^asxx)",
                   files,
                   count,
                   position));
}

/**
 * grooved_player_complete_add_track:
 * @object: A #GroovedPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.AddTrack">AddTrack()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
grooved_player_complete_add_track (
    GroovedPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * grooved_player_complete_add_list:
 * @object: A #GroovedPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.AddList">AddList()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
grooved_player_complete_add_list (
    GroovedPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * grooved_player_complete_goto_track:
 * @object: A #GroovedPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.GotoTrack">GotoTrack()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
grooved_player_complete_goto_track (
    GroovedPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * grooved_player_complete_remove_track:
 * @object: A #GroovedPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.RemoveTrack">RemoveTrack()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
grooved_player_complete_remove_track (
    GroovedPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * grooved_player_complete_loop:
 * @object: A #GroovedPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Loop">Loop()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
grooved_player_complete_loop (
    GroovedPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * grooved_player_complete_quit:
 * @object: A #GroovedPlayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-io-github-ghedo-grooved-Player.Quit">Quit()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
grooved_player_complete_quit (
    GroovedPlayer *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * GroovedPlayerProxy:
 *
 * The #GroovedPlayerProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * GroovedPlayerProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #GroovedPlayerProxy.
 */

struct _GroovedPlayerProxyPrivate
{
  GData *qdata;
};

static void grooved_player_proxy_iface_init (GroovedPlayerIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (GroovedPlayerProxy, grooved_player_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (GroovedPlayerProxy)
                         G_IMPLEMENT_INTERFACE (GROOVED_TYPE_PLAYER, grooved_player_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (GroovedPlayerProxy, grooved_player_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (GROOVED_TYPE_PLAYER, grooved_player_proxy_iface_init));

#endif
static void
grooved_player_proxy_finalize (GObject *object)
{
  GroovedPlayerProxy *proxy = GROOVED_PLAYER_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (grooved_player_proxy_parent_class)->finalize (object);
}

static void
grooved_player_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 5);
  info = _grooved_player_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
grooved_player_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface io.github.ghedo.grooved.Player: %s (%s, %d)",
                 info->parent_struct.name,
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
grooved_player_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 5);
  info = _grooved_player_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "io.github.ghedo.grooved.Player", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) grooved_player_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
grooved_player_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_grooved_player_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], GROOVED_TYPE_PLAYER);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, GROOVED_TYPE_PLAYER);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
grooved_player_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  GroovedPlayerProxy *proxy = GROOVED_PLAYER_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_grooved_player_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_grooved_player_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
grooved_player_proxy_get_playback_status (GroovedPlayer *object)
{
  GroovedPlayerProxy *proxy = GROOVED_PLAYER_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "PlaybackStatus");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
grooved_player_proxy_get_loop_status (GroovedPlayer *object)
{
  GroovedPlayerProxy *proxy = GROOVED_PLAYER_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "LoopStatus");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static GVariant *
grooved_player_proxy_get_track_metadata (GroovedPlayer *object)
{
  GroovedPlayerProxy *proxy = GROOVED_PLAYER_PROXY (object);
  GVariant *variant;
  GVariant *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "TrackMetadata");
  value = variant;
  if (variant != NULL)
    g_variant_unref (variant);
  return value;
}

static const gchar *
grooved_player_proxy_get_track_path (GroovedPlayer *object)
{
  GroovedPlayerProxy *proxy = GROOVED_PLAYER_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "TrackPath");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static gdouble
grooved_player_proxy_get_track_length (GroovedPlayer *object)
{
  GroovedPlayerProxy *proxy = GROOVED_PLAYER_PROXY (object);
  GVariant *variant;
  gdouble value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "TrackLength");
  if (variant != NULL)
    {
      value = g_variant_get_double (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
grooved_player_proxy_init (GroovedPlayerProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = grooved_player_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, GROOVED_TYPE_PLAYER_PROXY, GroovedPlayerProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), grooved_player_interface_info ());
}

static void
grooved_player_proxy_class_init (GroovedPlayerProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = grooved_player_proxy_finalize;
  gobject_class->get_property = grooved_player_proxy_get_property;
  gobject_class->set_property = grooved_player_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = grooved_player_proxy_g_signal;
  proxy_class->g_properties_changed = grooved_player_proxy_g_properties_changed;

  grooved_player_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (GroovedPlayerProxyPrivate));
#endif
}

static void
grooved_player_proxy_iface_init (GroovedPlayerIface *iface)
{
  iface->get_playback_status = grooved_player_proxy_get_playback_status;
  iface->get_loop_status = grooved_player_proxy_get_loop_status;
  iface->get_track_metadata = grooved_player_proxy_get_track_metadata;
  iface->get_track_path = grooved_player_proxy_get_track_path;
  iface->get_track_length = grooved_player_proxy_get_track_length;
}

/**
 * grooved_player_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-io-github-ghedo-grooved-Player.top_of_page">io.github.ghedo.grooved.Player</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call grooved_player_proxy_new_finish() to get the result of the operation.
 *
 * See grooved_player_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
grooved_player_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (GROOVED_TYPE_PLAYER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "io.github.ghedo.grooved.Player", NULL);
}

/**
 * grooved_player_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to grooved_player_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with grooved_player_proxy_new().
 *
 * Returns: (transfer full) (type GroovedPlayerProxy): The constructed proxy object or %NULL if @error is set.
 */
GroovedPlayer *
grooved_player_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return GROOVED_PLAYER (ret);
  else
    return NULL;
}

/**
 * grooved_player_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-io-github-ghedo-grooved-Player.top_of_page">io.github.ghedo.grooved.Player</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See grooved_player_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type GroovedPlayerProxy): The constructed proxy object or %NULL if @error is set.
 */
GroovedPlayer *
grooved_player_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (GROOVED_TYPE_PLAYER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "io.github.ghedo.grooved.Player", NULL);
  if (ret != NULL)
    return GROOVED_PLAYER (ret);
  else
    return NULL;
}


/**
 * grooved_player_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like grooved_player_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call grooved_player_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See grooved_player_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
grooved_player_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (GROOVED_TYPE_PLAYER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "io.github.ghedo.grooved.Player", NULL);
}

/**
 * grooved_player_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to grooved_player_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with grooved_player_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type GroovedPlayerProxy): The constructed proxy object or %NULL if @error is set.
 */
GroovedPlayer *
grooved_player_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return GROOVED_PLAYER (ret);
  else
    return NULL;
}

/**
 * grooved_player_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like grooved_player_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See grooved_player_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type GroovedPlayerProxy): The constructed proxy object or %NULL if @error is set.
 */
GroovedPlayer *
grooved_player_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (GROOVED_TYPE_PLAYER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "io.github.ghedo.grooved.Player", NULL);
  if (ret != NULL)
    return GROOVED_PLAYER (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * GroovedPlayerSkeleton:
 *
 * The #GroovedPlayerSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * GroovedPlayerSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #GroovedPlayerSkeleton.
 */

struct _GroovedPlayerSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_grooved_player_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  GroovedPlayerSkeleton *skeleton = GROOVED_PLAYER_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], GROOVED_TYPE_PLAYER);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, GROOVED_TYPE_PLAYER);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_grooved_player_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  GroovedPlayerSkeleton *skeleton = GROOVED_PLAYER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_grooved_player_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_grooved_player_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  GroovedPlayerSkeleton *skeleton = GROOVED_PLAYER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_grooved_player_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _grooved_player_skeleton_vtable =
{
  _grooved_player_skeleton_handle_method_call,
  _grooved_player_skeleton_handle_get_property,
  _grooved_player_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
grooved_player_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return grooved_player_interface_info ();
}

static GDBusInterfaceVTable *
grooved_player_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_grooved_player_skeleton_vtable;
}

static GVariant *
grooved_player_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  GroovedPlayerSkeleton *skeleton = GROOVED_PLAYER_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_grooved_player_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _grooved_player_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _grooved_player_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _grooved_player_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "io.github.ghedo.grooved.Player", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _grooved_player_emit_changed (gpointer user_data);

static void
grooved_player_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  GroovedPlayerSkeleton *skeleton = GROOVED_PLAYER_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _grooved_player_emit_changed (skeleton);
}

static void
_grooved_player_on_signal_status_changed (
    GroovedPlayer *object)
{
  GroovedPlayerSkeleton *skeleton = GROOVED_PLAYER_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("()"));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "io.github.ghedo.grooved.Player", "StatusChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_grooved_player_on_signal_track_changed (
    GroovedPlayer *object)
{
  GroovedPlayerSkeleton *skeleton = GROOVED_PLAYER_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("()"));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "io.github.ghedo.grooved.Player", "TrackChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_grooved_player_on_signal_option_changed (
    GroovedPlayer *object)
{
  GroovedPlayerSkeleton *skeleton = GROOVED_PLAYER_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("()"));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "io.github.ghedo.grooved.Player", "OptionChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void grooved_player_skeleton_iface_init (GroovedPlayerIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (GroovedPlayerSkeleton, grooved_player_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (GroovedPlayerSkeleton)
                         G_IMPLEMENT_INTERFACE (GROOVED_TYPE_PLAYER, grooved_player_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (GroovedPlayerSkeleton, grooved_player_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (GROOVED_TYPE_PLAYER, grooved_player_skeleton_iface_init));

#endif
static void
grooved_player_skeleton_finalize (GObject *object)
{
  GroovedPlayerSkeleton *skeleton = GROOVED_PLAYER_SKELETON (object);
  guint n;
  for (n = 0; n < 5; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (grooved_player_skeleton_parent_class)->finalize (object);
}

static void
grooved_player_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  GroovedPlayerSkeleton *skeleton = GROOVED_PLAYER_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 5);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_grooved_player_emit_changed (gpointer user_data)
{
  GroovedPlayerSkeleton *skeleton = GROOVED_PLAYER_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "io.github.ghedo.grooved.Player",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_grooved_player_schedule_emit_changed (GroovedPlayerSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
grooved_player_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  GroovedPlayerSkeleton *skeleton = GROOVED_PLAYER_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _grooved_player_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
grooved_player_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  GroovedPlayerSkeleton *skeleton = GROOVED_PLAYER_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 5);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _grooved_player_schedule_emit_changed (skeleton, _grooved_player_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
grooved_player_skeleton_init (GroovedPlayerSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = grooved_player_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, GROOVED_TYPE_PLAYER_SKELETON, GroovedPlayerSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 5);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_VARIANT);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_DOUBLE);
}

static const gchar *
grooved_player_skeleton_get_playback_status (GroovedPlayer *object)
{
  GroovedPlayerSkeleton *skeleton = GROOVED_PLAYER_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
grooved_player_skeleton_get_loop_status (GroovedPlayer *object)
{
  GroovedPlayerSkeleton *skeleton = GROOVED_PLAYER_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static GVariant *
grooved_player_skeleton_get_track_metadata (GroovedPlayer *object)
{
  GroovedPlayerSkeleton *skeleton = GROOVED_PLAYER_SKELETON (object);
  GVariant *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_variant (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
grooved_player_skeleton_get_track_path (GroovedPlayer *object)
{
  GroovedPlayerSkeleton *skeleton = GROOVED_PLAYER_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gdouble
grooved_player_skeleton_get_track_length (GroovedPlayer *object)
{
  GroovedPlayerSkeleton *skeleton = GROOVED_PLAYER_SKELETON (object);
  gdouble value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_double (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
grooved_player_skeleton_class_init (GroovedPlayerSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = grooved_player_skeleton_finalize;
  gobject_class->get_property = grooved_player_skeleton_get_property;
  gobject_class->set_property = grooved_player_skeleton_set_property;
  gobject_class->notify       = grooved_player_skeleton_notify;


  grooved_player_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = grooved_player_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = grooved_player_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = grooved_player_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = grooved_player_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (GroovedPlayerSkeletonPrivate));
#endif
}

static void
grooved_player_skeleton_iface_init (GroovedPlayerIface *iface)
{
  iface->status_changed = _grooved_player_on_signal_status_changed;
  iface->track_changed = _grooved_player_on_signal_track_changed;
  iface->option_changed = _grooved_player_on_signal_option_changed;
  iface->get_playback_status = grooved_player_skeleton_get_playback_status;
  iface->get_loop_status = grooved_player_skeleton_get_loop_status;
  iface->get_track_metadata = grooved_player_skeleton_get_track_metadata;
  iface->get_track_path = grooved_player_skeleton_get_track_path;
  iface->get_track_length = grooved_player_skeleton_get_track_length;
}

/**
 * grooved_player_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-io-github-ghedo-grooved-Player.top_of_page">io.github.ghedo.grooved.Player</link>.
 *
 * Returns: (transfer full) (type GroovedPlayerSkeleton): The skeleton object.
 */
GroovedPlayer *
grooved_player_skeleton_new (void)
{
  return GROOVED_PLAYER (g_object_new (GROOVED_TYPE_PLAYER_SKELETON, NULL));
}

